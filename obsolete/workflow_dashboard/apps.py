from django.apps import AppConfig

class WorkflowDashboardConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "plugins.workflow_dashboard"
    verbose_name = "Workflow Dashboard Plugin"
